# Zero-table extraction triage

Source: `tests/json/summary.json` (generated by TREX batch extraction on `tests/pdfs/*.pdf`).

## Completed fixes

- `twotables.pdf`
  - Before: `0` tables
  - Now: `2` tables
  - Camelot check: `2` tables across lattice/stream/network/hybrid
  - Applied: Form XObject(`Do`) 내부 Content 파싱 + 좌표 변환 지원.
  - Added regression test: `tests/regression.rs`.

## Product decision applied

- `health_protected.pdf`
  - Before: `0` tables (silent)
  - Now: 명시적 실패 (`암호화된 PDF는 현재 지원하지 않습니다`)
  - Camelot check: throws `FileNotDecryptedError` without password.
  - Applied: 비밀번호 PDF 미지원 정책으로 고정.

## Remaining zero-table files (expected)

- `empty.pdf`
  - TREX: `0` tables
  - Interpretation: expected for empty content.

- `image.pdf`
  - TREX: `0` tables
  - Camelot check: image-based warning with `0` tables.
  - Interpretation: expected without OCR support.

- `only_page_number.pdf`
  - TREX: `0` tables
  - Camelot stream can over-detect (`1` table), other flavors return `0`.
  - Interpretation: current TREX result is acceptable unless broader recall target changes.
